[project]
name = "rag-evaluation"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.31",
    "azure-ai-inference>=1.0.0b9",
    "azure-identity>=1.17",
    "python-dotenv>=1.0",
]

[tool.uv]
prerelease = "allow"
